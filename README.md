# nextjs template

## Requirements

* docker
* docker-compose

## Installation

```
$ docker-compose build
```

## Run

```
# dev mode
$ docker-compose up

# in other terminal
$ curl -i localhost:3000/example/sample | head -1
HTTP/1.1 200 OK
```